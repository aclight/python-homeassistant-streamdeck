# Environment variables for python-homeassistant-streamdeck
# Copy this to .env and fill in your values
# Never commit .env to version control!

# Home Assistant API credentials
# Use a long-lived access token (recommended) or API password (deprecated)
HASS_API_TOKEN=your-long-lived-token-here
# HASS_API_PASSWORD=your-api-password-here  # (legacy, not recommended)

# Usage notes:
# 1. Copy this file to .env in your project or working directory:
#
#    cp .env.example .env
#
# 2. (Optional) Install python-dotenv support so the CLI will automatically
#    load the .env file when you run the installed package:
#
#    python -m pip install .[env]
#    # or
#    pip install python-dotenv
#
# 3. Run the CLI pointing to your configuration file (see config.example.yaml):
#
#    hass-streamdeck --config ./config.yaml

# Reminder: keep .env out of version control. Add a line to .gitignore if needed:
#
#    echo ".env" >> .gitignore
