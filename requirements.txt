# requirements.txt has been intentionally deprecated for this project.
# Dependency metadata is maintained in `pyproject.toml`.
# Use `python -m pip install .[dev]` to install runtime + dev/test dependencies.
